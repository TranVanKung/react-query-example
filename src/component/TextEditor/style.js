import styled from "styled-components";

const TextEditorWrapper = styled.div`
  border: 1px solid var(--color-border);
  border-radius: var(--border-radius);
  padding: 1rem 1rem;
  min-height: 20rem;
`;

export { TextEditorWrapper };

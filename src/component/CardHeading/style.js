import styled from "styled-components";

const CardHeadingWrapper = styled.div`
  font-weight: 600;
  font-size: 1.6rem;
  color: var(--color-text-secondary);
  text-transform: uppercase;
`;

export { CardHeadingWrapper };

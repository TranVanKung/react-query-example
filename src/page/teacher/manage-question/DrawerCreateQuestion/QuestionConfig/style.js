import styled from "styled-components";

const QuestionConfigWrapper = styled.div`
  background-color: white;
  border-radius: var(--border-radius);
  padding: var(--padding-page);
`;

export { QuestionConfigWrapper };

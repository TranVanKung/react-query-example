import styled from "styled-components";

const ListExamPaperWrapper = styled.div`
  .heading {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    margin-bottom: var(--margin-bottom);
  }
`;

export { ListExamPaperWrapper };
